npm run dev #То есть в данном случае выполняем команду, которая определена в секции scripts и которая запускает приложение
npm run build #Эта команда по файлу app/app.jsx создаст в проекте папку public, а в ней - новый файл bundle.js. Это именно тот файл, который будет подключаться на веб-страницу.